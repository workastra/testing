import { type Config } from 'prettier';

const config: Config = {
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'always',
  tabWidth: 2,
  useTabs: false,
  printWidth: 100,
  bracketSpacing: true,
  jsxSingleQuote: true,
  endOfLine: 'lf',
  quoteProps: 'as-needed',
  semi: true,
};

export default config;
